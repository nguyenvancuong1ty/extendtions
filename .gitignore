# API capture exports can contain credentials from intercepted requests.
/Gg_flow/Google_Flow_APIs_*.json

# Local Git metadata retained when incorporating a formerly standalone project.
/.git-metadata-backups/
